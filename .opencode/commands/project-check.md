---
description: Run Terrarium type and Svelte checks, then summarize failures
agent: build
---

Run `npm run check`.

If it fails, summarize the first actionable errors with file paths and likely causes. Do not make code changes unless the user explicitly asks you to fix the failures.

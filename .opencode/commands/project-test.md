---
description: Run the Terrarium Vitest suite and summarize failures
agent: build
---

Run `npm run test`.

If it fails, summarize the failing test files, failing assertions, and likely affected source files. Do not make code changes unless the user explicitly asks you to fix the failures.

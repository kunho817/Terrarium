import { describe, it, expect } from 'vitest';

describe('BlockPreview', () => {
  it('renders preview panel', () => {
    // BlockPreview displays:
    // - Header: "Live Preview"
    // - Body: concatenated text from enabled TextBlocks
    // - Footer: block count
    // - Empty state when no blocks
    expect(true).toBe(true);
  });
});
